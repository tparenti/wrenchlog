
services:
  wrenchlog:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    # Base compose serves the built frontend from the image.
    # Use docker-compose.override.yml for local development with Vite + HMR.
