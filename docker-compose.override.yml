services:
  wrenchlog:
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
  frontend:
    volumes:
      - ./frontend:/app:delegated
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: ["npm", "run", "dev", "--", "--host"]
